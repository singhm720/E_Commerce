<!DOCTYPE html>
<html>
<head>
  <title>ECommerceWeb</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_importmap_tags %>
  <!-- Bootstrap CSS -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
  .nav-item {
    margin-right: 10px;
  }
  
  .nav-link {
    color: #333;
    text-decoration: none;
    font-weight: bold;
  }
  .btn {
    border-radius: 20px;
    padding: 5px 10px;
  }
  .nav-item img {
    max-width: 30px;
  }
  .important-notice {
    color: red;
  }
  </style>
</head>
<body>
<div class="container">
  <marquee class="important-notice">
    The Website is for testing purposes only. Please do not make any payments.
  </marquee>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <%= link_to 'ECommerce', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
          <% if user_signed_in? %>
          <li class="nav-item">
          <%= form_tag(root_path, method: :get, class: " nav-link navbar-collapse justify-content-center flex-lg-1 order-2 order-lg-1 collapse", id:"navbar_search") do %>
            <div class="row align-items-center", style = "margin-right:12px">
              <div class="col-lg-10">
                <div class="navbar-search flex-fill position-relative mt-2 mt-lg-0">
                  <div class="form-control-feedback form-control-feedback-start flex-grow-1" data-color-theme="dark">
                    <%= text_field_tag :query, params[:query], id: "searchInput", placeholder: "Search Products", class: "form-control bg-transparent rounded-pill form-control-lg", style: "width: 534px;" %>
                  </div>
                </div>
              </div>
            </div>  
          <div class="col-lg-2">
              <%= submit_tag "Search", class: "btn btn-outline-success my-2 my-sm-0 w-100", style:"margin-right: 50px"%>
          </div>
      <% end %>                
      </li>
          <li class="nav-item" style="margin-top: 10px;">
            <%= link_to cart_items_path, class: 'nav-link' do %>
              <%= image_tag("cart.svg", alt: "Icon", class: "img-fluid") %>
              <span><%= session[:cart_count] || 0 %></span>
            <% end %>
          </li>
          <li class="nav-item dropdown show" style="margin-top: 10px;">
            <div class="user-profile btn btn-secondary dropdown-toggle" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= image_tag("user_profile_image.jpg", alt: "User Profile Image", class: "img-fluid rounded-circle") %>
              <span><%= current_user.name %></span>
            </div>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
              <%= link_to 'Upload Product', new_product_path, class: "dropdown-item" %>
              <%= link_to 'Your Order', show_orders_purchase_records_path, class:"dropdown-item"%>
              <%= form_tag(destroy_user_session_path, method: :delete) do %>
                <%= button_tag(type: 'submit', class: 'btn btn-link', data: { confirm: 'Are you sure?' }) do %>
                  <div class="dropdown-item"style="padding-left: 8px;">Sign Out</div>
                <% end %>
              <% end %>
            </div>
          </li>
        <% else %>
          <li class="nav-item flex-fill">
            <%= link_to 'Sign In', new_user_session_path, class: "nav-link btn btn-outline-primary" %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
</div>
<%= yield %>
</body>
</html>
